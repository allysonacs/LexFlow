/**
 * Classificação determinística do tipo de demanda (Prompt 08).
 *
 * <p>Nenhuma classe daqui chama LLM: a classificação é feita por palavras-chave, com regras que se
 * testam isoladamente e dão sempre o mesmo resultado para a mesma entrada.
 */
package com.lexflow.domain.classification;
