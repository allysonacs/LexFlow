/**
 * Núcleo do domínio do LexFlow: entidades, value objects, regras puras e a máquina de estados
 * de {@code LegalCaseStatus}.
 *
 * <p>Este módulo não pode depender de nenhum framework (Spring, JPA, Jackson etc.). Toda regra
 * aqui deve ser testável com JUnit puro.
 */
package com.lexflow.domain;
