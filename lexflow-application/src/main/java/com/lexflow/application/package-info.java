/**
 * Camada de aplicação do LexFlow: casos de uso e portas (interfaces) que a infraestrutura e a API
 * implementam.
 *
 * <p>Depende exclusivamente de {@code lexflow-domain}. Não conhece banco, fila, storage nem HTTP.
 */
package com.lexflow.application;
