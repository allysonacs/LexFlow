/**
 * Versões de prompt: todo texto enviado ao LLM vem de um template registrado em banco, para que cada
 * resposta possa ser rastreada até o prompt exato que a produziu (seção 10, item 5).
 */
package com.lexflow.application.prompt;
