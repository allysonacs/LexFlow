/**
 * Autenticação e autorização da API.
 *
 * <p>Por ora protege apenas a área administrativa ({@code /api/v1/admin/**}), exigida pelo Prompt 09.
 * O controle de acesso por papel das demais rotas (seção 12) ainda não foi implementado.
 */
package com.lexflow.api.security;
