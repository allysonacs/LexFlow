/**
 * Observabilidade: métricas das integrações externas, correlação por demanda e métrica de
 * concordância entre a IA e o revisor humano (Prompt 18).
 *
 * <p>Nada aqui registra conteúdo de documento, texto de prompt ou de resposta — só metadados,
 * contagens e tempos (seção 12).
 */
package com.lexflow.infrastructure.observability;
