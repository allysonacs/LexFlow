/**
 * Trilha de auditoria (Prompt 16).
 *
 * <p>A gravação acontece em decoradores dos repositórios que já existiam, e não dentro dos casos de
 * uso: assim nenhuma regra de negócio precisou ser alterada para ser auditada, e a restrição do
 * Prompt 16 — a trilha observa, não participa — fica estrutural, e não apenas combinada.
 */
package com.lexflow.infrastructure.audit;
